/**
 * Schema Types Index
 *
 * Exports all content schemas for the Sanity Studio.
 * Right now there's just "post", but you can add more
 * types here later (like "project" or "page").
 */

import post from "./post";

export const schemaTypes = [post];
